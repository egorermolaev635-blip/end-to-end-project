# Optional: required only if you want src/llm_summary.py to call an LLM API.
# Do not put real keys into Git.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=https://api.openai.com/v1/chat/completions

# PostgreSQL connection for local load.py.
DB_URL=postgresql+psycopg2://analytics:analytics_pass@localhost:5432/analytics_db
