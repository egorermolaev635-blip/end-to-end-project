# Неделя 1. Старт: Conda через .bat, Git, структура репозитория

| **Чекпоинт** | **Открывается** | **Дедлайн** |
|--------------|-----------------|-------------|
| `week1`      | 2026-02-16      | 2026-03-01  |

## Цели

- ✅ Поставить Anaconda и убедиться, что `conda` работает
- ✅ Сделать воспроизводимый запуск на Windows одной командой: `scripts/setup_env.bat`
- ✅ Создать GitHub-репозиторий и базовую структуру data-проекта
- ✅ Завести документы в `docs/` и правила по LLM (лог)

---

**Статус:** 🟢 Готово | **Прогресс:** 4/4

# Неделя 2. HTTP и API: requests, таймауты, обработка ошибок, сохранение raw JSON

| **Чекпоинт** | **Открывается** | **Дедлайн** |
|--------------|-----------------|-------------|
| `week2`      | 2026-03-02      | 2026-03-15  |

## Цели

- ✅ Написать `broken_requests.py` и зафиксировать проблемы зависания/ошибок
- ✅ Исправить код: добавить `timeout`, обработку исключений, проверку статуса/JSON
- ✅ Протестировать на медленных ответах (delay/10), 404, HTML-ответах
- ✅ Создать `src/**/extract.py`: чтение `configs/variant_03.yml`
- ✅ Сделать GET-запрос к Open-Meteo API по координатам из YAML
- ✅ Сохранить raw JSON в `data/raw/variant_03/YYYY-MM-DD_HH-MM-SS.json`
- ✅ Добавить логи: вариант, URL, статус, путь сохранения
- ✅ Обновить `docs/Data_Contract.md`

---

**Статус:** 🟢 Готово | **Прогресс:** 8/8
## Неделя 3. Pandas: DataFrame, типы данных, чтение JSON, первичная очистка

| Чекпоинт | Открывается | Дедлайн   |
|----------|------------|-----------|
| `week3`  | 2026-03-09 | 2026-03-22 |

---

### Цели

- ✅ Разобрать структуру raw JSON и определить зерно таблицы (1 строка = 1 час наблюдения)
- ✅ Построить DataFrame из данных `hourly` (`time`, `temperature_2m`)
- ✅ Проверить структуру данных: `df.head()`, `df.shape`, `df.columns`, `df.dtypes`
- ✅ Привести типы данных (`time` → datetime, `temperature` → float)
- ✅ Проверить и обработать пропуски (`df.isna().sum()`, при необходимости `dropna`)
- ✅ Добавить дополнительные признаки (`date`, `hour`)
- ✅ Сохранить normalized данные в `data/normalized/variant_XX/YYYY-MM-DD_HH-MM-SS.csv` (`index=False`)
- ✅ Обновить `docs/Data_Contract.md` (схема normalized-слоя, типы, nullable, описание)

---

**Статус:** 🟢 Готово | **Прогресс:** 8/8

# Неделя 4. Группировки, join и временные ряды

| Чекпоинт | Открывается | Дедлайн   |
|----------|------------|-----------|
| `week4`  | 2026-03-16 | 2026-03-31 |

---

### Цели

- ✅ Проверить normalized-файл: структура, колонки, типы данных
- ✅ Определить KPI, объект анализа и дневную гранулярность
- ✅ Добавить поле `city_id` из config
- ✅ Подготовить справочник `reference/cities.csv`
- ✅ Выполнить join с проверкой кардинальности (`many-to-one`)
- ✅ Убедиться, что число строк после merge не изменилось
- ✅ Построить витрину через `groupby`
- ✅ Рассчитать KPI по температуре
- ✅ Сохранить mart в `data/mart/variant_03/`
- ✅ Обновить документацию (Data Contract, Implementation Plan)

---

### Что сделано

- данные normalized агрегированы по дням
- добавлен `city_id = RU_NSK` из config
- создан минимальный справочник `cities.csv`
- выполнен join по `city_id`
- проверено, что строки не размножились
- построена дневная витрина

---

### KPI

- `temperature_mean` — средняя температура за день  
- `temperature_min` — минимальная температура  
- `temperature_max` — максимальная температура  
- `temperature_range` — диапазон температуры  

---

### Результат

Витрина сохранена в:
data/mart/variant_03/mart_daily_YYYY-MM-DD_HH-MM-SS.csv

---

### Итог

Собран полный pipeline:
raw JSON → normalized CSV → mart CSV

---

**Статус:** 🟢 Готово | **Прогресс:** 10/10

# Неделя 5. SQL и база данных: загрузка в PostgreSQL, проверки и KPI

| Чекпоинт | Открывается | Дедлайн   |
|----------|------------|-----------|
| `week5`  | 2026-03-23 | 2026-04-07 |

---

### Цели

- ✅ Поднять PostgreSQL (Docker) и проверить доступность базы
- ✅ Настроить подключение к базе (`host`, `port`, `database`, `user`, `password`)
- ✅ Реализовать скрипт `src/load.py` для загрузки mart в БД
- ✅ Загрузить витрину в таблицу `mart_weather`
- ✅ Использовать транзакцию (`engine.begin()`) и понять роль commit
- ✅ Обеспечить идемпотентную загрузку (`if_exists="replace"`)
- ✅ Проверить, что таблица не пустая
- ✅ Выполнить SQL-проверки качества данных (`COUNT`, `MIN/MAX`, `NULL`, дубли, KPI)
- ✅ Создать `docs/sql_checks.md` с набором проверок

---

### Что сделано

- поднят PostgreSQL через Docker
- настроено подключение к базе данных
- реализован `load.py` для загрузки витрины
- mart-файл успешно загружен в таблицу `mart_weather`
- использована транзакция через `engine.begin()`
- обеспечена идемпотентность загрузки (`replace`)
- выполнены SQL-запросы для проверки данных

---

### SQL-проверки

- проверено, что таблица не пустая (`COUNT`)
- проверен диапазон дат (`MIN/MAX`)
- проверены NULL в ключевых колонках
- проверены дубли по (`date`, `city_id`)
- проверены агрегированные метрики температуры

---

### Результат

данные загружены в PostgreSQL  
таблица: `mart_weather`  
SQL-запросы успешно выполняются  
данные прошли базовые проверки качества  

---

### Итог

pipeline расширен:  
raw JSON → normalized CSV → mart CSV → PostgreSQL → SQL checks  

---

**Статус:** 🟢 Готово | **Прогресс:** 9/9
# Неделя 6. ETL-дизайн: слои, идемпотентность и инкрементальные загрузки

| Чекпоинт | Открывается | Дедлайн   |
|----------|------------|-----------|
| `week6`  | 2026-03-30 | 2026-04-14 |

---

### Цели

- ✅ Объединить все этапы в единый pipeline (`src/pipeline.py`)
- ✅ Реализовать единый CLI-запуск пайплайна
- ✅ Поддержать режимы `full` и `incremental`
- ✅ Устранить хардкод путей (raw / normalized / mart)
- ✅ Реализовать автоматическое связывание слоёв
- ✅ Добавить `state.json` для хранения состояния
- ✅ Определить и реализовать `watermark`
- ✅ Обеспечить идемпотентность пайплайна
- ✅ Обновить `docs/Implementation_Plan.md`

---

### Что сделано

- реализован единый pipeline (`pipeline.py`)
- все стадии (extract → transform → mart → load) запускаются одной командой
- добавлена поддержка режимов:
  - `--mode full`
  - `--mode incremental`
- реализован запуск ноутбука через `nbconvert`
- убран хардкод входных и выходных файлов
- каждая стадия использует последний доступный файл из предыдущего слоя
- добавлен файл состояния `data/state.json`
- реализовано сохранение:
  - последнего режима запуска
  - времени последнего успешного запуска
  - watermark
  - пути к последнему raw-файлу
- реализован расчет watermark как максимальной даты из mart
- обновление state выполняется только после успешного завершения pipeline

---

### Слои данных

- `data/raw/variant_03/` — исходные данные API (JSON)
- `data/normalized/variant_03/` — очищенные табличные данные
- `data/mart/variant_03/` — агрегированная витрина

---

### Watermark

- используется поле: `date`
- определяется как максимальная дата в mart
- обновляется после успешного выполнения всех этапов

Пример:
watermark = 2026-04-14

---

### Business key

Одна строка витрины = один день по одному городу

Business key:
date + city_id

---

### Идемпотентность

- повторный запуск пайплайна не изменяет результат при одинаковых входных данных
- загрузка в PostgreSQL выполняется через `if_exists="replace"`
- дубликаты не накапливаются

---

### Режимы работы

#### Full

- выполняются все этапы заново
- все слои пересоздаются
- витрина полностью пересобирается
- таблица в БД заменяется

#### Incremental

- используется `state.json`
- сохраняется и используется watermark
- пайплайн можно безопасно запускать повторно
- результат не дублируется

---

### Результат

pipeline автоматизирован и управляется одной командой  
реализована логика state и watermark  
обеспечена идемпотентность  
поддерживаются режимы full и incremental  

---

### Итог

Собран полноценный ETL pipeline:

raw JSON → normalized CSV → mart CSV → PostgreSQL

Добавлены ключевые инженерные свойства:

- единая точка входа  
- воспроизводимость  
- идемпотентность  
- хранение состояния (state)  
- поддержка incremental  

---

**Статус:** 🟢 Готово | **Прогресс:** 9/9

# Неделя 7. Визуализация данных: matplotlib и data storytelling

| Чекпоинт | Открывается | Дедлайн   |
|----------|------------|-----------|
| `week7`  | 2026-04-06 | 2026-04-21 |

---

### Цели

- ✅ Построить временной ряд (line plot) по метрике  
- ✅ Построить распределение (histogram)  
- ✅ Построить ranking (bar chart)  
- ✅ Обеспечить корректную работу с датой (`datetime + сортировка`)  
- ✅ Добавить подписи осей, заголовки и единицы измерения  
- ✅ Сформулировать выводы по графикам  
- ✅ Использовать самый свежий mart-файл  

---

### Что сделано

- создан ноутбук `notebooks/week7_viz.ipynb`  
- реализована автоматическая загрузка последнего CSV из `data/mart/variant_03/`  
- данные приведены к корректным типам (`date → datetime`)  
- выполнена сортировка по времени  
- построены 3 графика:
  - временной ряд по `temperature_mean`  
  - распределение `temperature_mean`  
  - bar chart по `temperature_range`  
- добавлены краткие выводы по результатам визуализации  

---

### Загрузка данных

- используется последний файл из:
data/mart/variant_03/*.csv  
- выбор файла выполняется автоматически по времени изменения  

---

### Визуализации

1. Временной ряд  
   - динамика средней температуры по датам  

2. Распределение  
   - распределение значений температуры  

3. Ranking  
   - сравнение температуры по диапазонам  

---

### Результат

- построены 3 корректных графика  
- данные проанализированы через визуализацию  
- соблюдены требования к оформлению графиков  
- ноутбук воспроизводим  

---

### Итог

Добавлен этап анализа данных:

mart → matplotlib → выводы  

---

**Статус:** 🟢 Готово | **Прогресс:** 7/7

# Неделя 8. Data Quality: проверки данных и unit-тесты

| Чекпоинт | Открывается | Дедлайн   |
|----------|------------|-----------|
| `week8`  | 2026-04-13 | 2026-04-28 |

---

### Цели

- ✅ Реализовать проверки качества данных для слоя mart  
- ✅ Написать отдельный модуль `src/dq.py`  
- ✅ Проверить таблицу на пустоту  
- ✅ Проверить NULL в критических колонках  
- ✅ Проверить уникальность бизнес-ключа  
- ✅ Проверить диапазоны значений  
- ✅ Проверить логические условия между колонками  
- ✅ Реализовать формирование JSON-отчёта  
- ✅ Добавить unit-тесты через pytest  

---

### Что сделано

- реализован модуль `dq.py` для проверки качества данных  
- автоматически выбирается последний mart-файл из `data/mart/variant_03/`  
- данные загружаются в DataFrame  
- реализованы проверки:
  - таблица не пустая  
  - отсутствие NULL в ключевых полях  
  - уникальность (date, city_id)  
  - диапазон температур  
  - корректность min/max температур  
  - допустимые значения country_code  
- результаты проверок собираются в единый отчёт  
- отчёт сохраняется в `data/dq_report.json`  
- добавлен вывод результата в консоль  
- реализована остановка выполнения при наличии FAIL  
- написаны unit-тесты `tests/test_dq.py`  
- протестированы сценарии:
  - пустая таблица  
  - NULL значения  
  - дубликаты  
  - выход за диапазон  
  - нарушение логики  

---

### Структура

- `src/dq.py` — проверки качества данных  
- `tests/test_dq.py` — unit-тесты  
- `data/dq_report.json` — результат проверок  

---

### Запуск

DQ-проверки:

python src/dq.py  

Тесты:

python3 -m pytest tests  

---

### Результат

- сформирован JSON-отчёт по качеству данных  
- все проверки возвращают статус (PASS / FAIL / WARNING)  
- тесты успешно проходят  
- логика проверок подтверждена  

---

### Итог

Добавлен слой Data Quality:

mart CSV → DQ проверки → JSON отчёт  

Обеспечено:

- контроль качества данных  
- воспроизводимость проверок  
- тестируемость логики  
- прозрачность результатов  

# Неделя 9. Data Governance: Data Contract, Data Dictionary, единицы и версии

| Чекпоинт | Открывается | Дедлайн   |
|----------|------------|-----------|
| `week9`  | 2026-04-20 | 2026-05-05 |

---

### Цели

- ✅ Описать полный Data Contract (schema + semantics)
- ✅ Зафиксировать единицы измерения (units)
- ✅ Зафиксировать часовой пояс (timezone)
- ✅ Ввести правила именования (naming conventions)
- ✅ Добавить версию контракта и changelog
- ✅ Описать гранулярность данных
- ✅ Создать Data Dictionary (человеко-ориентированное описание)
- ✅ Согласовать контракт и код

---

### Что сделано

- оформлен полный Data Contract (`docs/Data_Contract.md`)
- добавлены:
  - версия контракта (`version`)
  - история изменений (`changelog`)
  - единицы измерения (°C, degrees)
  - часовой пояс (`Asia/Novosibirsk`)
  - правила именования колонок
  - ограничения (constraints)
  - гранулярность данных
- описаны слои:
  - normalized (почасовые данные)
  - mart (дневная витрина)
- создан словарь данных (`docs/data_dictionary.md`)
- зафиксирован бизнес-смысл всех колонок mart

---

### Data Contract

Файл:
docs/Data_Contract.md  

Содержит:

- описание источника данных (Open-Meteo API)
- параметры запроса
- схемы слоёв:
  - normalized
  - mart
- типы данных и nullable
- единицы измерения:
  - температура → °C  
  - координаты → degrees  
- часовой пояс: `Asia/Novosibirsk`
- гранулярность:
  - normalized → 1 строка = 1 час  
  - mart → 1 строка = 1 день × 1 город  
- ключи:
  - `date + city_id`
- ограничения на данные
- правила именования
- версия и changelog

---

### Data Dictionary

Файл:
docs/data_dictionary.md  

Содержит:

- человеко-ориентированное описание колонок mart
- бизнес-смысл метрик
- единицы измерения
- пояснения к интерпретации

---

### Гранулярность

- normalized:
  1 строка = одно почасовое наблюдение  

- mart:
  1 строка = агрегированные показатели за один день по одному городу  

---

### Основная идея

Данные приведены к управляемому виду:

- структура данных зафиксирована  
- смысл колонок описан  
- единицы измерения определены  
- время интерпретируется однозначно  
- изменения схемы контролируются через version + changelog  

---

### Результат

Pipeline дополнен уровнем Data Governance:

API → raw → normalized → mart → PostgreSQL → DQ → visualization → **Data Contract + Data Dictionary**

Обеспечено:

- согласованность данных  
- понятность структуры  
- прозрачность изменений  
- готовность к использованию в аналитике и BI  

---

### Итог

Проект перешёл от этапа "данные просто есть" к этапу:

"данные описаны, стандартизированы и управляемы"

# Неделя 10. Docker: контейнеризация PostgreSQL, Metabase и BI-дашборд

| Чекпоинт | Открывается | Дедлайн   |
|----------|------------|-----------|
| `week10` | 2026-04-27 | 2026-05-12 |

---

### Цели

- ✅ Добавить `docker-compose.yml` для запуска PostgreSQL и Metabase
- ✅ Поднять сервисы одной командой через Docker Compose
- ✅ Подключить volume для PostgreSQL, чтобы данные не пропадали после пересоздания контейнера
- ✅ Подключить volume для Metabase, чтобы сохранялись настройки и дашборды
- ✅ Загрузить mart-витрину в PostgreSQL через существующий `src/load.py`
- ✅ Проверить, что таблица `mart_weather` находится в базе данных, а не только в CSV
- ✅ Подключить Metabase к PostgreSQL
- ✅ Собрать BI-дашборд из трёх визуализаций
- ✅ Сохранить скриншоты дашборда в `docs/bi/`

---

### Что сделано

- добавлен файл `docker-compose.yml`
- описан сервис `postgres` на базе образа `postgres:16`
- описан сервис `metabase` на базе образа `metabase/metabase:latest`
- для PostgreSQL добавлен volume `pgdata`
- для Metabase добавлен volume `metabase_data`
- сервисы запускаются одной командой:
  - `docker compose up -d`
- проверка контейнеров выполняется командой:
  - `docker compose ps`
- mart-витрина загружается в PostgreSQL через уже существующий скрипт `src/load.py`
- данные загружаются в таблицу `mart_weather`
- Metabase подключён к PostgreSQL внутри Docker Compose-сети
- создан дашборд `Weather Analytics Dashboard`
- сохранены BI-скриншоты в `docs/bi/`

---

### Docker Compose

Файл:
`docker-compose.yml`

В compose-файле описаны два сервиса:

| Сервис | Назначение |
|--------|------------|
| `postgres` | база данных PostgreSQL для хранения mart-витрины |
| `metabase` | BI-инструмент для построения дашборда |

---

### Volumes

В проекте используются два volume:

| Volume | Назначение |
|--------|------------|
| `pgdata` | хранит данные PostgreSQL |
| `metabase_data` | хранит настройки и дашборды Metabase |

Главная идея недели:

контейнеры можно пересоздавать, но данные должны храниться отдельно.

Поэтому данные PostgreSQL не должны храниться только внутри контейнера. Они сохраняются в volume `pgdata`.

---

### Запуск

Запуск сервисов:

```bash
docker compose up -d
```

Проверка запущенных контейнеров:

```bash
docker compose ps
```

После запуска доступны:

```text
PostgreSQL: localhost:5432
Metabase:   http://localhost:3000
```

---

### Загрузка mart-витрины в PostgreSQL

Для загрузки данных используется существующий скрипт:

```bash
python src/load.py
```

Скрипт берёт последний mart-файл из папки:

```text
data/mart/variant_03/
```

и загружает данные в таблицу:

```text
mart_weather
```

Подключение в `src/load.py`:

```text
database: analytics_db
user: analytics
password: analytics_pass
host: localhost
port: 5432
```

---

### Проверка таблицы в PostgreSQL

Подключение к базе внутри контейнера:

```bash
docker compose exec postgres psql -U analytics -d analytics_db
```

Проверочные SQL-команды:

```sql
\dt
SELECT COUNT(*) FROM mart_weather;
SELECT * FROM mart_weather LIMIT 5;
```

---

### Подключение Metabase

Metabase подключается к PostgreSQL со следующими параметрами:

```text
Database type: PostgreSQL
Host: postgres
Port: 5432
Database name: analytics_db
Username: analytics
Password: analytics_pass
```

Важно:

`Host = postgres`, потому что Metabase работает внутри Docker Compose-сети и видит PostgreSQL по имени сервиса.

При этом `src/load.py` использует `localhost:5432`, потому что запускается с локальной машины, а порт PostgreSQL проброшен наружу.

---

### BI-дашборд

В Metabase создан дашборд:

```text
Weather Analytics Dashboard
```

Источник данных:

```text
PostgreSQL → analytics_db → public → mart_weather
```

---

### Визуализации

В дашборд добавлены 3 визуализации:

1. Средняя температура по дням  
   - тип: line chart  
   - поля: `date`, `temperature_mean`

2. Максимальная температура по дням  
   - тип: bar chart  
   - поля: `date`, `temperature_max`

3. Сводка температурной витрины  
   - тип: table  
   - поля: `date`, `city_name`, `temperature_mean`, `temperature_min`, `temperature_max`, `temperature_range`

Так как текущая mart-витрина содержит небольшой объём данных, третья визуализация сделана в виде таблицы. Это позволяет показать итоговые строки витрины без искусственного усложнения графиков.

---

### BI-артефакты

Скриншоты сохранены в папке:

```text
docs/bi/
```

Файлы:

```text
docs/bi/dashboard_overview.png
docs/bi/chart_timeseries.png
docs/bi/chart_summary.png
```

Описание:

| Файл | Содержание |
|------|------------|
| `dashboard_overview.png` | общий вид дашборда в Metabase |
| `chart_timeseries.png` | график средней температуры по дням |
| `chart_summary.png` | табличная сводка mart-витрины |

---

### Проверка volumes

Проверка созданных volumes:

```bash
docker volume ls
```

Проверка контейнеров:

```bash
docker compose ps
```

Разница между командами:

- `docker compose stop` — останавливает контейнеры, но не удаляет их
- `docker compose down` — удаляет контейнеры, но сохраняет volumes
- `docker compose down -v` — удаляет контейнеры и volumes, поэтому данные PostgreSQL и настройки Metabase пропадут

---

### Результат

PostgreSQL и Metabase поднимаются одной командой через Docker Compose.  
Mart-витрина загружается в таблицу `mart_weather` в PostgreSQL.  
Metabase подключается к PostgreSQL и строит дашборд по таблице из базы данных, а не по CSV-файлу.  
Скриншоты BI-дашборда сохранены в `docs/bi/`.

---

### Итог

Pipeline расширен до BI-стенда:

API → raw → normalized → mart → PostgreSQL → Metabase → BI Dashboard

Добавлены ключевые элементы воспроизводимой среды:

- Docker Compose
- PostgreSQL container
- Metabase container
- volumes для хранения данных и настроек
- BI-дашборд по таблице из PostgreSQL
- артефакты в `docs/bi/`

---

**Статус:** 🟢 Готово | **Прогресс:** 9/9
